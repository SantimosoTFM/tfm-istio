apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: kube-gw
  namespace: istio-system
spec:
  selector:
    istio: ingressgateway
  servers:
  - port:
      number: 80 # 31500
      name: kube-http
      protocol: HTTP2
    hosts:
    - "kube-system/*.kube.santier.tfm"
