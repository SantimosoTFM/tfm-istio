apiVersion: v1
kind: Service
metadata:
  namespace: {{ k8sNamespace }}
  name: network-super-tools
  labels:
    app: network-super-tools
spec:
  ports:
  - port: 80
    name: http
  selector:
    app: network-super-tools